#include <stdio.h>

int main() {
  int number;
    while(number != -9999)
    {
         scanf("%d", &number);
        switch(number != -9999)
        {
            case 1: printf("%d ", number);
                break;
        }
    }
  return 0;
}